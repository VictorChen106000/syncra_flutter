import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Core brand
  static const Color ink = Color(0xFF000100);
  static const Color accent = Color(0xFFA0FE08);
  static const Color accentBright = Color(0xFF8BD90A);

  // Surfaces
  static const Color scaffold = Color(0xFFEAECEB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color softSurface = Color(0xFFF4F5F4);
  static const Color subtleSurface = Color(0xFFFAFAFA);
  static const Color border = Color(0xFFD1D3D2);

  // Text
  static const Color textMuted = Color(0xFF666666);
  static const Color textSoft = Color(0xFF999999);

  // Navigation
  static const Color navBackground = Color(0xFF1C1C1E);
  static const Color navInactive = Color(0xFF8E8E93);

  // Status / system
  static const Color danger = Color(0xFFFF5F56);
  static const Color warning = Color(0xFFFFBD2E);
  static const Color success = Color(0xFF27C93F);

  // Login gradient stops
  static const Color loginGradientTop = Color(0xFF312E81);
  static const Color loginGradientMid = Color(0xFF581C87);

  // OS Home gradient stops
  static const Color osGradientTop = Color(0xFF0F172A);
  static const Color osGradientMid = Color(0xFF3B0764);
}
