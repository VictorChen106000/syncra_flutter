Add these dependencies to pubspec.yaml:

dependencies:
  provider: ^6.1.2
  go_router: ^14.8.1
  file_picker: ^8.3.7
  intl: ^0.19.0

Add these asset folders later when you have images:

flutter:
  assets:
    - assets/images/
    - assets/logo/
